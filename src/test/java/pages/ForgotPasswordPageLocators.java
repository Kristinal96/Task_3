package pages;

import org.openqa.selenium.WebDriver;

public class ForgotPasswordPageLocators {
    private WebDriver driver;

    // Локатор для ссылки "Войти"
    public static final String LOGIN_LINK = "//a[contains(text(), 'Войти')]";
}